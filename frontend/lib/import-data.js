const axios = require('axios');
const log = require('mk-log');
const fs = fs.promises;
const path = require('path');


async function import() {
  try {

    const path = require(
    const results = path. a
  } catch (err) {
    log.error(err);
    return Promise.error(err); 
  }
}

module.exports = import; 
