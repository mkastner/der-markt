# Install svelte for development via npx

1. install npx with yarn(npm) globally
   yarn global add npx
   (npm install -g npx)
   
2. install degit with yarn/npm globally
   yarn global add degit 
   (npm install -g degit) 

